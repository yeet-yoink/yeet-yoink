[package]
name = "metrics"
version = "0.1.0"
edition = "2024"

[dependencies]
http = "1.4.1"
lazy_static = "1.5.0"
prometheus-client = "0.24.1"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
